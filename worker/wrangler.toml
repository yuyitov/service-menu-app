name = "service-menu-worker"
main = "worker.js"
compatibility_date = "2024-01-01"

# ─── KV Namespace ────────────────────────────────────────────────────────────
# Para obtener el id desde Cloudflare Dashboard:
# → Workers & Pages → KV → SERVICE_MENU_KV → Copy ID
# O en terminal: npx wrangler kv namespace list
#
# PASOS:
# 1. Crea el namespace en Cloudflare: https://dash.cloudflare.com/?to=/:account/workers/kv/namespaces
# 2. Copia el namespace ID aquí
[[kv_namespaces]]
binding = "SERVICE_MENU_KV"
id      = "0743b2af26cd47fb88d4fef62e90a118"

# ─── Secrets (NO van aquí — se configura con: wrangler secret put) ────────────
# Estos secrets deben configurarse después via:
#   npx wrangler secret put STRIPE_WEBHOOK_SECRET
#   npx wrangler secret put TALLY_SIGNING_SECRET_EN
#   npx wrangler secret put TALLY_SIGNING_SECRET_ES
#   npx wrangler secret put GITHUB_TOKEN
#   npx wrangler secret put SENDGRID_API_KEY
#   npx wrangler secret put NOTIFY_SECRET
#
# Secrets requeridos:
# - STRIPE_WEBHOOK_SECRET — obtén de: Stripe Dashboard → Webhooks → Signing secret
# - TALLY_SIGNING_SECRET_EN — signing secret del webhook del form yPkN5X (cada form de Tally genera el suyo)
# - TALLY_SIGNING_SECRET_ES — signing secret del webhook del form MeyDpk
# - GITHUB_TOKEN — GitHub Personal Access Token (repo dispatch scope)
# - SENDGRID_API_KEY — obtén de: https://sendgrid.com/login → API Keys
# - NOTIFY_SECRET — genera uno: openssl rand -base64 32 (solo para ti, guardar seguro)
# - STRIPE_SECRET_KEY — OPCIONAL: activa la compra de correcciones adicionales
#   (/buy-correction crea Checkout Sessions al vuelo). Crear en Stripe una key
#   RESTRINGIDA con permiso Write solo en "Checkout Sessions" y subirla con:
#     npx wrangler secret put STRIPE_SECRET_KEY
#   Sin esta key, el botón de compra cae a la página de contacto (WhatsApp).

# ─── Environment Variables (no-secret) ────────────────────────────────────────
[vars]
# ID(s) del Payment Link de HMU Link (plink_...). Acepta varios separados por
# coma — p.ej. el link USD ($39) y el MXN ($699) — para que todas las compras de
# HMU se procesen, ignorando ventas de otros productos de la cuenta (ej. MyGuest).
# Vacío = filtro apagado (el worker loguea el plink observado para capturarlo).
# Al crear el link MXN, agrega su plink id aquí:  "plink_USD,plink_MXN"
STRIPE_PAYMENT_LINK_ID = "plink_1TpW1pG4Nw60erBPpTk296oO,plink_1Tr7lEG4Nw60erBPZ51f4cOC,plink_1TuOQXG4Nw60erBPUus5heVm,plink_1TuOvXG4Nw60erBPQPwonRRP"
GITHUB_REPO          = "yuyitov/service-menu-app"
GITHUB_ACTIONS_EVENT = "new-hmu-service-menu"
TALLY_FORM_URL_EN    = "https://tally.so/r/yPkN5X?order_id="
TALLY_FORM_URL_ES    = "https://tally.so/r/MeyDpk?order_id="
FROM_EMAIL           = "HMU Link <hello@hmulink.com>"
# hello@ no es buzón real; las respuestas (p.ej. corrección gratuita) van aquí.
REPLY_TO_EMAIL       = "veronica.perezarroyo@gmail.com"
PUBLIC_BOOK_BASE_URL = "https://www.hmulink.com"
# Modificaciones gratis incluidas en la compra. Estandar de la casa desde el
# 2026-07-20 (decision de Vero): 2. Es la MISMA fuente que imprimen los
# Terminos publicados, la landing y los correos del worker — si este numero y
# el texto legal divergen, HMU promete algo que no entrega. Regla de oro:
# primero se despliega el worker con el numero nuevo, DESPUES el legal.
FREE_CHANGES         = "2"
# URL pública de este worker — la usan los links de "comprar corrección
# adicional" (/buy-correction) que van en correos y en la página /correct/.
WORKER_PUBLIC_URL    = "https://service-menu-worker.veronica-perezarroyo.workers.dev"
# Prefill de prospecto (Cory → worker, Fase 2.4 de la Link Factory): base
# pública donde Cory publica el prefill.json de cada prospecto. El worker lee
# `<base>/<slug>/prefill.json` cuando la compra llegó por un link rastreado
# (client_reference_id=<slug>) y prellena el formulario de Tally del correo
# post-pago con los datos del prospecto. Quitar/vaciar la var apaga el prefill
# sin ningún otro cambio (fail-open).
PROSPECT_PREFILL_BASE_URL = "https://preview.hmulink.com/p"

